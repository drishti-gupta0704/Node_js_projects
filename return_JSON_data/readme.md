
## This project demonstrates how to create a basic HTTP server using Node.js core modules (without Express) and return JSON data for a specific route

## 🚀 Features
- Uses Node.js built-in http module
- Handles routing manually
- Responds with JSON data
- Sets proper HTTP status codes and headers
- Includes a fallback 404 route