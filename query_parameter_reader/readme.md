
# Query Parameters Reader (Node.js)

This is a simple Node.js HTTP server that demonstrates how to read and log query parameters from a URL using the built-in URL class

## 📌 Features
- Uses Node.js http module (no external libraries)
- Reads query parameters from incoming requests
- Logs key–value pairs to the console

## 🛠️Technologies Used
- Node.js
- JavaScript