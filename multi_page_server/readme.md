
## Multi-Page Node.js Server

This is a basic multi-page HTTP server built using Node.js core http module.
It handles different routes and sends different responses based on the URL

## 🚀 Features

- Built using pure Node.js (no Express)
- Handles multiple routes:
- / → Home page
- /about → About page
- /login → Login page
- Displays a 404 error for unknown routes
